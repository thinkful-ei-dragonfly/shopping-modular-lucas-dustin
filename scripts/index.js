/* global shoppingList, store, foo, cuid */

// eslint-disable-next-line no-unused-vars
'use strict';

$(document).ready(function() {
  shoppingList.bindEventListeners();
  shoppingList.render();
  console.log(Item);
  console.log(foo);
});
